# Example — DO NOT commit real secrets.
# Apply with: kubectl apply -f secret.yaml  (after copying this and filling in)
# For real use: kubectl create secret generic parakram-secrets --from-env-file=.env.production
apiVersion: v1
kind: Secret
metadata:
  name: parakram-secrets
  namespace: parakram
type: Opaque
stringData:
  SUPABASE_DB_URL: "postgresql://..."
  SUPABASE_JWT_SECRET: "..."
  SUPABASE_URL: "https://xxx.supabase.co"
  ANTHROPIC_API_KEY: "sk-ant-..."
  ANTHROPIC_MODEL: "claude-sonnet-4-6"
  STRIPE_SECRET_KEY: "sk_live_..."
  STRIPE_WEBHOOK_SECRET: "whsec_..."
  STRIPE_PRICE_HOBBY: "price_..."
  STRIPE_PRICE_PRO: "price_..."
